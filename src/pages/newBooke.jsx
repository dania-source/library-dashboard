import React from "react";
import { Box, Typography, } from "@mui/material";
const newBooke = () => {
 return (
          <Box>
            <Typography variant="h4" sx={{ color: "#541029", mb: 2 }}>
              إدارة الكتب
            </Typography>
            <Typography>هنا يمكنك إضافة وحذف وتعديل الكتب.</Typography>
          </Box>
        );}
        export default newBooke;